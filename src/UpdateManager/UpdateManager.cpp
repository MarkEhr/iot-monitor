#include "UpdateManager.h"

UpdateManager::UpdateManager( Configuration* config ){
    // set up
}

void UpdateManager::loop() {
    // Polling logic with interval
}

void UpdateManager::update(String binaryName) {
    // Trigger update
}